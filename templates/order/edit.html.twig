{% extends 'base.html.twig' %}

{% block title %}Cartlify | Edit Order{% endblock %}

{% block body %}
<div class="main-content">
    <div class="form-container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-warning">Update Order</button>
        {{ form_end(form) }}
        <a href="{{ path('app_order_index') }}" class="back-link">← Back to Orders</a>
    </div>
</div>
{% endblock %}
